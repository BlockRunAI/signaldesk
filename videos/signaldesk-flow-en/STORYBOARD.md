# Continuous English workflow · 24 seconds

One persistent scene, no slide cuts. 1920×1080, 60 fps, silent social playback.

- 0–3s: Product URL and all three stage headings enter with a short settle.
- 1.5–7.9s: Six selected original X post excerpts scroll smoothly in a masked
  feed; the verified recommendation request settles at the top.
- 8–9.5s: Highlight the matched post. A compact request label crosses to Jev.
- 9.5–12.5s: Intent, fit and confidence bars reveal. Priority and measured
  classification time appear. These are the selected post's actual run scores.
- 12.5–16.5s: The evidence-linked opportunity arrives; the original generated
  reply draft unfolds below it.
- 18–24s: Headline settles to the outcome; the full workflow holds for reading.

Motion: continuous `sine.inOut` feed movement, `power2.inOut` handoff,
`power3.out` panel settling. No camera jumps, hard cuts or decorative bouncing.
A single paused GSAP timeline drives render and the interactive player.

Review: scrolling uses intentional overflow/occlusion in its feed mask. The
single-scene nesting lint advisory is accepted because this is deliberately one
continuous scene. No layout/runtime/contrast errors. Motion and proof frames
were inspected before render. Backend's 14 regression tests pass.
